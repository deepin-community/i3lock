Subject: nit: do not increase failed_attempts beyond 999
Origin: backport, https://github.com/i3/i3lock/pull/369

Index: i3lock/i3lock.c
===================================================================
--- i3lock.orig/i3lock.c
+++ i3lock/i3lock.c
@@ -310,7 +310,11 @@ static void input_done(void) {
         fprintf(stderr, "Authentication failure\n");
 
     auth_state = STATE_AUTH_WRONG;
-    failed_attempts += 1;
+    /* The unlock indicator displays the number of failed attempts,
+     * but caps the attempts to 999, so only increment up to 999. */
+    if (failed_attempts < 999) {
+        failed_attempts += 1;
+    }
     clear_input();
     if (unlock_indicator)
         redraw_screen();
